# Used automatically by `npm run dev` (local development).
# Vite picks this file for "development" mode; no manual switching needed.
VITE_API_BASE_URL=http://localhost:8000/api
VITE_QR_ENGINE_WS_URL=http://localhost:4000
